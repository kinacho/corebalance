# Efficiency Checklist

- [ ] Check if the required code symbol is defined in `entities.yaml` or `types.ts` before reading the source file.
- [ ] Limit terminal command outputs by specifying filters (e.g. `npx vitest run -- <file>`).
- [ ] Use `git diff` to review modifications instead of reading the updated file.
- [ ] Prefer English instructions to limit token size.
- [ ] Keep responses concise and machine-readable.
- [ ] Update `current-state.md` immediately after merging or modifying code routes.
